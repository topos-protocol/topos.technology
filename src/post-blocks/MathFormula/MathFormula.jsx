import React from "react";
import styled from "@emotion/styled";

const Content = styled.div`
  
`;

export default function MathFormula({ postBlock }) {
  return (
    <Content layout={postBlock?.layout}>
    </Content>
  );
}
